#include "main.h"

/**
 * _strpbrk - searches a string for set of bytes.
 * @s: var to use.
 * @accept: containing the byte to look for.
 * Return: point to the byte in s.
 */
char *_strpbrk(char *s, char *accept)
{
}
